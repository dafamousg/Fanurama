import { NotCharactersPipe } from './not-characters.pipe';

describe('NotCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new NotCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
